import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'Gautham Tours and Travels - Premium Cab Services',
  description: 'Experience premium chauffeur-driven cab services throughout Maharashtra. Outstation trips, local travel, airport transfers, hotel booking and complete trip planning services.',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  )
}